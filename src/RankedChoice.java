import java.util.ArrayList;

public class RankedChoice extends VotingSystem {
    public RankedChoice(ArrayList<Candidate> candidates, int numOfVoters) {
        super(candidates, numOfVoters);
    }
}
